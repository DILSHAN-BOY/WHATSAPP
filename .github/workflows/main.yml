name: WhatsApp Bot Auto Build 🚀

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔥 Checkout Code
      uses: actions/checkout@v3

    - name: 🟢 Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: "20"

    - name: 📦 Install Dependencies
      run: npm install

    - name: 🧪 Run Tests (Optional)
      run: echo "No test scripts configured"

    - name: ⚙️ Start and Keep Running
      run: |
        npm install pm2 -g
        pm2 start index.js --name "whatsapp-bot"
        pm2 save
        pm2 startup
